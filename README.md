Тестовый проект, мини-СУБД по учёту технических средств ТКДРМ.
Разрабатывается в свободное от основной работы время.
На настоящее время разработка идёт с частичным наполнением БД. В открытом доступе залитой БД нет. Есть только её структура, описание моделей и рисунок схемы (ниже). У разработчика на данный момент есть около 1500 валидированных записей таблицы Device, при планируемом в проде их количестве около 20000.

Технологии: Python, Django, MySQL (перспектива - PostgreSQL)

Done:
- структура БД, всё более и более точно описывающая реальные сущности. На настоящее время более 30 таблиц.
- management-команда, парсер легаси БД из Excel, только по первым 1500 объектам.
- две первые view-функции, общие перечень объектов и детализация одного объекта.
- пагинация, кастомная, самописная.

ToDo:
- апдейтить БД по мере подгрузки легаси-данных.
- авторизацию.
- админку.
- создать оставшееся по отдельному объекту (Create, Update)
- создать вторую страницу отображения полного перечня объектов, с иными настройками сортировки.
- перевод на PostgreSQL.
- перевести фронт с шаблонов на API.

Структура БД в виде схемы приведена в файле Untitled.pdf. Будет корректироваться по мере разработки.

- git clone репозиторий.
- Создать venv. Установить в него зависимости.
- cd tkdrm1
- python manage.py makemigrations
- python manage.py migrate

Обеспечить наличие в ./Tkdrm/Tkdrm1 файла базы данных db.sqlite3. Для этого можно извлечь её из архива на пароле, либо можно сформировать встроенным парсером (обеспечив наличие в той же папке источника данных __база СТСО.xlsm, время формирования от 10 до 40 минут, команда на запуск python manage.py import3).

- python manage.py runserver

Автор: Дмитрий, контакт в профиле github.